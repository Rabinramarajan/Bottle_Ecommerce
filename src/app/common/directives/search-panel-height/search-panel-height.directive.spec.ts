import { SearchPanelHeightDirective } from './search-panel-height.directive';

describe('SearchPanelHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchPanelHeightDirective();
    expect(directive).toBeTruthy();
  });
});
