import { EmptySpaceHandlerDirective } from './empty-space-handler.directive';

describe('EmptySpaceHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptySpaceHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
